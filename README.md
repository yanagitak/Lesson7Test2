# QR コード生成ツール（Flask）

Render でそのまま公開できる、最小構成の Python 製 QR コード生成 Web アプリです。  
フォームに入力したテキストから QR コード（PNG）を生成し、画面表示・ダウンロードできます。

---

## ローカル実行

```bash
python -m venv env
source env/bin/activate
# Windows の場合: env\Scripts\activate

pip install -r requirements.txt
python app.py
```
